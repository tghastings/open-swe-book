name: release-epubs

# Tag a version (e.g. `git tag 1.0b2 && git push --tags`, with or without a "v")
# and this builds
# the five per-language EPUB editions plus a PDF twin of each, AND the print-ready
# LaTeX interior ("Print Edition - Generic.pdf"), and publishes them as release
# assets. Stable download URLs (always the newest release):
#   https://github.com/tghastings/open-swe-book/releases/latest/download/swebook-python.epub
#   https://github.com/tghastings/open-swe-book/releases/latest/download/swebook-python.pdf
#   https://github.com/tghastings/open-swe-book/releases/latest/download/Print.Edition.-.Generic.pdf
#
# The print edition is set in Amazon Endure. The .otf files are NOT in the repo
# (licensing); the workflow downloads them at build time from Amazon's public KDP
# font page (the same files KDP ships). No secret or manual setup required.
on:
  push:
    tags: ["v*", "[0-9]*"]   # accepts v1.0b2 or a bare 1.0b2

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Stamp the tag version into the hardcoded files (title page, README, etc.)
      # in the workspace so the release PDF/EPUB assets carry it — the tagged
      # commit predates the version-sync commit on main, so inject at build time.
      - name: Sync version to the tag (workspace, for the built assets)
        run: |
          python3 tools/bump-version.py "${GITHUB_REF_NAME#v}" \
            || echo "::warning::version bump skipped for ${GITHUB_REF_NAME} (unexpected tag format?)"

      - name: Install mdBook + mdbook-mermaid
        run: |
          MDBOOK_VERSION="0.4.40"
          MERMAID_VERSION="0.14.0"
          curl -sSL "https://github.com/rust-lang/mdBook/releases/download/v${MDBOOK_VERSION}/mdbook-v${MDBOOK_VERSION}-x86_64-unknown-linux-gnu.tar.gz" \
            | tar -xz --directory=/usr/local/bin
          curl -sSL "https://github.com/badboy/mdbook-mermaid/releases/download/v${MERMAID_VERSION}/mdbook-mermaid-v${MERMAID_VERSION}-x86_64-unknown-linux-gnu.tar.gz" \
            | tar -xz --directory=/usr/local/bin

      - name: Install pandoc (pinned — apt's 3.1 emits MathML that fails epubcheck)
        run: |
          curl -sL -o /tmp/pandoc.deb \
            "https://github.com/jgm/pandoc/releases/download/3.10/pandoc-3.10-1-amd64.deb"
          sudo dpkg -i /tmp/pandoc.deb
          pandoc --version | head -1

      - name: Build the five EPUB editions and their PDF twins
        env:
          EPUB_OUT: ${{ github.workspace }}/dist
          CHROME_FLAGS: --no-sandbox
          SWEBOOK_VERSION: ${{ github.ref_name }}
        run: bash tools/build-epubs.sh

      - name: Validate every EPUB with epubcheck
        run: |
          curl -sL -o /tmp/epubcheck.zip \
            "https://github.com/w3c/epubcheck/releases/download/v5.1.0/epubcheck-5.1.0.zip"
          unzip -q /tmp/epubcheck.zip -d /tmp
          for f in dist/*.epub; do
            echo "== $f"
            java -jar /tmp/epubcheck-5.1.0/epubcheck.jar "$f"
          done

      - name: Build the print interior (LaTeX / xelatex, Amazon Endure)
        env:
          CHROME_FLAGS: --no-sandbox
          LATEX_NATIVE: "1"
          ENDURE_URL: "https://m.media-amazon.com/images/G/01/kindle-publication/EN/Amazon_Endure_font_EN.zip"
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            texlive-xetex texlive-latex-extra texlive-latex-recommended \
            texlive-fonts-recommended texlive-plain-generic texlive-bibtex-extra biber \
            librsvg2-bin fontconfig lmodern imagemagick
          # Amazon Endure is not redistributed in this repo; fetch it at build time
          # from Amazon's public KDP font page (the same files KDP ships).
          mkdir -p latex/fonts
          curl -sSL --retry 3 -o /tmp/endure.zip "$ENDURE_URL"
          unzip -j -o /tmp/endure.zip \
            "Amazon Endure font EN/AmazonEndure-Book.otf" \
            "Amazon Endure font EN/AmazonEndure-SemiBold.otf" \
            "Amazon Endure font EN/AmazonEndure-BookItalic.otf" \
            "Amazon Endure font EN/AmazonEndure-SemiBoldItalic.otf" \
            -d latex/fonts
          count=$(ls latex/fonts/AmazonEndure-*.otf 2>/dev/null | wc -l)
          if [ "$count" -ne 4 ]; then
            echo "::error::Expected 4 Endure .otf files, got $count — the Amazon font zip URL or layout may have changed ($ENDURE_URL)."
            exit 1
          fi
          ls latex/fonts/*.otf
          bash latex/book-build.sh   # exits non-zero if the PDF is not produced
          echo "print interior built: $(du -h latex/swebook-interior.pdf | cut -f1)"
          mkdir -p dist
          cp latex/swebook-interior.pdf "dist/Print Edition - Generic.pdf"

      - name: Build & bundle the KDP print cover wrap
        env:
          CHROME_FLAGS: --no-sandbox
        run: |
          # cover-wrap.html uses Georgia (Liberation Serif fallback in CI) + DejaVu Mono.
          # ImageMagick already installed by the interior step; ensure the fonts + allow
          # ImageMagick to WRITE PDFs (Ubuntu's default policy blocks the PDF coder).
          # poppler-utils + python3-pil let check-cover.py rasterize and validate geometry.
          sudo apt-get install -y --no-install-recommends \
            fonts-liberation fonts-dejavu-core poppler-utils python3-pil
          POLICY="$(find /etc/ImageMagick-* -name policy.xml 2>/dev/null | head -1)"
          [ -n "$POLICY" ] && sudo sed -i 's/rights="none" pattern="PDF"/rights="read|write" pattern="PDF"/' "$POLICY" || true
          if bash latex/make-cover.sh; then
            # Validate KDP dimensions + spine/barcode safe zones before shipping the asset.
            python3 latex/check-cover.py latex/swebook-generic-cover-wrap.pdf \
              || echo "::warning::cover geometry check reported issues (see log above)"
            cp latex/swebook-generic-cover-wrap.pdf "dist/Print Cover - Generic.pdf"
            echo "cover wrap: $(du -h latex/swebook-generic-cover-wrap.pdf | cut -f1)"
          else
            echo "::warning::cover wrap build failed — release will omit Print Cover - Generic.pdf"
          fi

      - name: Publish as release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          V="${GITHUB_REF_NAME#v}"   # release tag stays as pushed; title shows the clean version
          gh release create "$GITHUB_REF_NAME" \
            --title "Software Engineering: Standing on the Shoulders of Giants $V" \
            --notes "EPUB and PDF editions of [swebook.org](https://www.swebook.org), one per code-example language (prose is identical across editions), plus **Print Edition - Generic.pdf** (the print-ready 7x10 interior, pseudocode edition, Amazon Endure) and **Print Cover - Generic.pdf** (the KDP case-laminate cover wrap)." \
            --verify-tag dist/*.epub dist/*.pdf \
          || gh release upload "$GITHUB_REF_NAME" dist/*.epub dist/*.pdf --clobber
